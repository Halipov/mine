# Mine Launcher

Минимальный лаунчер для модовой сборки Minecraft на Fabric. Друг скачивает
один файл, вводит ник и жмёт «Играть»: лаунчер сам приносит нужную версию
игры, Java, загрузчик Fabric и моды, а сервер уже стоит в списке сетевой игры.

Аккаунт Mojang не нужен — вход оффлайновый, по нику.

## Что умеет

- **Обновляется сам.** Состав модов, версия игры и адрес сервера живут в
  манифесте на GitHub. Меняешь манифест — у всех при следующем запуске
  подтягивается новое. Обновлять сам лаунчер для этого не требуется.
- **Не трогает чужое.** Лаунчер помнит, какие файлы положил он сам, и удаляет
  только их. Мод, который игрок докинул в `mods` вручную, переживёт любое
  обновление сборки.
- **Приносит свою Java.** Скачивает JRE нужной версии с Adoptium под нужную
  платформу, включая Apple Silicon. Системную Java не трогает и не требует.
- **Профили.** Несколько ников с переключением. UUID считается так же, как в
  самом Minecraft (`OfflinePlayer:<ник>`), поэтому сервер узнаёт того же
  игрока после переустановки.
- **Память.** Ползунок с потолком по объёму памяти машины и разумными
  флагами G1 для модовой сборки.
- **Сервер в списке.** Пишется в `servers.dat` первым, свои серверы игрока
  сохраняются. Есть галочка «подключиться сразу» — минуя главное меню.

## Быстрый старт

```bash
npm install
npm run dev
```

Проверить, что весь конвейер работает, не запуская игру:

```bash
npm run selftest           # без ресурсов игры, ~200 МБ
npm run selftest -- --full # вместе с ресурсами
```

## Репозиторий

Всё живёт в одном репозитории: код лаунчера, манифест сборки `pack.json`
в корне и релизы — и лаунчера, и файлов сборки.

Два требования, без которых у друзей ничего не заработает:

- **Репозиторий должен быть публичным.** Лаунчер ходит за манифестом на
  `raw.githubusercontent.com`, а за модами — в ассеты релизов; в приватном
  репозитории и то и другое отдаётся только по токену, и друзья получат 404.
  Ничего секретного в репозитории и нет — разве что адрес сервера, но его
  всё равно знают все, кто играет.
- **Ветка должна называться `main`** — на неё ссылается адрес манифеста
  в [config.ts](src/shared/config.ts). Хочешь другую — поправь там же.

Теги разведены по смыслу и не мешают друг другу:

| Тег | Что это |
|---|---|
| `v0.1.0` | релиз лаунчера, собирается автоматикой |
| `pack-2026-09-01` | файлы сборки, если есть моды не с Modrinth |

Самообновление ищет только теги вида `v1.2.3`, так что релиз сборки его
не собьёт.

## Настройка под себя

### 1. Лаунчер

[`src/shared/config.ts`](src/shared/config.ts) — имя приложения и репозиторий,
откуда берутся манифест сборки и обновления самого лаунчера.

### 2. Сборка

[`pack/pack.config.json`](pack/pack.config.json) — версия Minecraft, адрес
сервера, список модов:

```json
{
  "id": "smp",
  "name": "Наш сервер",
  "minecraft": "1.21.1",
  "server": { "name": "Наш сервер", "host": "play.example.com", "port": 25565 },
  "modrinth": ["fabric-api", "sodium", "lithium", "modmenu"]
}
```

Моды с Modrinth указываются слагом из адреса страницы мода — заливать их
никуда не нужно, у них есть постоянный CDN. Версия подбирается автоматически
под указанный Minecraft; чтобы закрепить конкретную, пиши
`{ "slug": "sodium", "version": "mc1.21.1-0.6.0" }`.

Мод не с Modrinth — положи jar в `pack/mods/`, он уедет ассетом в релиз.
Общие конфиги — в `pack/files/` (перезаписываются при каждом обновлении),
личные настройки игрока — в `pack/files-once/` (ставятся один раз).

## Как менять состав модов

Весь процесс — правка одного списка и одна команда.

**Добавить мод.** Найти его на Modrinth, взять слаг из адреса страницы
(`modrinth.com/mod/`**`sodium`**) и дописать в `modrinth` в
[pack/pack.config.json](pack/pack.config.json).

**Убрать мод.** Удалить строку оттуда же.

**Пересобрать манифест:**

```bash
npm run pack:build
```

Скрипт покажет, что именно изменилось:

```
  fabric-api 0.116.17+1.21.1
  sodium mc1.21.1-0.8.13-fabric
  lithium mc1.21.1-0.15.4-fabric

Изменения:
  + lithium-fabric-0.15.4+mc1.21.1.jar
  − modmenu-11.0.4.jar
  ~ sodium-fabric-0.6.0.jar → sodium-fabric-0.8.13+mc1.21.1.jar

Записан pack.json (pack-2026-09-02)
  модов: 4, доп. файлов: 0
  общий вес: 4.2 МБ
```

`+` добавлен, `−` убран, `~` обновилась версия. Тег по умолчанию —
сегодняшняя дата, свой задаётся через `--tag`.

**Опубликовать:**

```bash
git add pack.json && git commit -m "убрал modmenu" && git push
```

Всё. У друзей новый состав подтянется при следующем запуске лаунчера: он
докачает добавленное, удалит убранное и не тронет моды, которые они положили
в папку сами.

### Что скрипт делает за тебя

- **Тянет обязательные зависимости.** Добавил Mod Menu — вместе с ним
  приедут Fabric API и Placeholder API. Забытая зависимость иначе
  оборачивается «у меня игра не запускается» в личке уже после того, как
  все обновились.
- **Подбирает версию** под указанный в конфиге Minecraft, предпочитая
  релизные. Закрепить конкретную: `{ "slug": "sodium", "version": "mc1.21.1-0.6.0" }`.
- **Считает SHA-1.** Лаунчер по ним понимает, какие файлы качать заново,
  а какие уже лежат на диске нетронутые.

### Мод не с Modrinth

Положить jar в `pack/mods/` — он уедет ассетом в релиз, скрипт напечатает
готовую команду `gh release create`. Только для таких файлов и нужен
заполненный блок `github` в конфиге; сборке целиком с Modrinth он не
требуется вовсе.

### Обкатать до публикации

Поле «Адрес манифеста сборки» в настройках лаунчера понимает и ссылку, и путь
к локальному файлу. Указав `C:\Work\mine\pack.json`, можно проверить новую
сборку на себе, ничего не отправляя друзьям.

### Смена версии Minecraft

Поменять `minecraft` в конфиге и пересобрать манифест. Скрипт сам подберёт
версии всех модов под новую игру и честно упадёт с понятной ошибкой, если
какой-то мод под неё ещё не вышел. Лаунчер скачает другую версию игры и, если
нужно, другую Java — старая останется на диске, так что откат назад бесплатный.

## Выпуск лаунчера

Нужен, только когда меняется сам лаунчер, а не состав модов.

```bash
npm version patch
git push --tags
```

[GitHub Actions](.github/workflows/release.yml) соберёт Windows-инсталлятор и
macOS-бандл на своих раннерах и выложит их в релиз. Лаунчер у друзей найдёт
обновление во вкладке «О лаунчере».

Локально: `npm run pack:win` или `npm run pack:mac` (только на своей платформе —
`.app` с Windows не собрать).

## Если не запускается или не собирается

**`npm : Невозможно загрузить файл npm.ps1, выполнение сценариев отключено`.**
Политика выполнения PowerShell, к проекту отношения не имеет. Либо звать npm
батником — `npm.cmd run dev`, — либо один раз разрешить скрипты для своего
пользователя (без прав администратора):

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

**`Cannot create symbolic link ... libcrypto.dylib` при сборке.**
electron-builder распаковывает пакет `winCodeSign`, внутри которого лежат
символические ссылки для macOS, а Windows не даёт их создавать без прав
администратора или режима разработчика. Распаковать пакет в кеш заранее,
пропустив ненужные на Windows файлы:

```powershell
$cache = "$env:LOCALAPPDATA\electron-builder\Cache\winCodeSign"
New-Item -ItemType Directory -Force -Path $cache | Out-Null
Invoke-WebRequest -OutFile "$cache\winCodeSign-2.6.0.7z" `
  'https://github.com/electron-userland/electron-builder-binaries/releases/download/winCodeSign-2.6.0/winCodeSign-2.6.0.7z'
& .\node_modules\7zip-bin\win\x64\7za.exe x "$cache\winCodeSign-2.6.0.7z" `
  "-o$cache\winCodeSign-2.6.0" '-xr!darwin'
```

Найдя готовую папку в кеше, electron-builder не станет качать пакет заново.
На раннерах GitHub Actions этой проблемы нет — там символические ссылки
создаются свободно.

**Окно Electron не открывается, а в консоли ошибка про `app.setName`.**
В терминале редактора живёт переменная `ELECTRON_RUN_AS_NODE=1`, из-за которой
Electron стартует как обычный Node. Все npm-скрипты идут через
[scripts/run.mjs](scripts/run.mjs), который её вычищает; если запускаешь
`electron` руками — сбрось её сам.

## macOS

Сертификата Apple Developer у проекта нет, отсюда две особенности.

**Первый запуск.** Приложение подписано ad-hoc — на Apple Silicon без этого
оно бы вообще не запустилось. Останется обычное предупреждение о неизвестном
разработчике: правый клик по приложению → «Открыть» → «Открыть». Один раз.

**Обновление.** Штатный механизм Electron требует настоящей подписи, поэтому
лаунчер обновляет себя сам: качает zip из релиза, подменяет бандл и
перезапускается. Файл, скачанный самим приложением, карантин не получает,
так что Gatekeeper к обновлению не придирается.

## Где лежат данные

- Windows — `%APPDATA%\Mine Launcher`
- macOS — `~/Library/Application Support/Mine Launcher`

Внутри: `shared/` (версии, библиотеки, ресурсы — общие для всех сборок),
`runtime/` (скачанная Java), `instances/<id>/` (папка игры: моды, конфиги,
сохранения), `settings.json`.

## Устройство

```
src/main/core/
  http.ts        загрузка: пул потоков, проверка хешей, докачка, ретраи
  mojang.ts      version_manifest, version.json, client.jar, ресурсы
  fabric.ts      meta-API Fabric: версия загрузчика и профиль
  version.ts     правила Mojang, слияние профилей, разбор библиотек
  java.ts        JRE с Adoptium под платформу
  natives.ts     распаковка нативных библиотек (для версий до 1.19)
  sync.ts        синхронизация модов с реестром «наших» файлов
  servers.ts     запись servers.dat (NBT) с сохранением чужих серверов
  launch.ts      подстановка аргументов, classpath, запуск JVM
  installer.ts   весь конвейер по шагам
```

## Ограничения

- Только Fabric. Forge и NeoForge требуют прогона installer-процессоров с
  патчингом клиента — это отдельная и сильно большая история.
- Только оффлайн-вход. Для игры на своём сервере с `online-mode=false` этого
  достаточно; на официальные серверы с таким лаунчером не попасть.
